version: '3.8'
services:
  uploader:
    build:
      context: .
      dockerfile: Dockerfile.dev
    container_name: uploader
    expose:
      - ${PORT}
    volumes:
      - .:/app
      - /app/node_modules
    env_file:
      - '.env'
    ports:
      - 35000:${PORT}
    restart: unless-stopped
